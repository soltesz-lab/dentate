; -*- Hen -*-

((egg "cellconfig.egg") ; This should never change

 ; List here all the files that should be bundled as part of your egg.  

 (files "cellconfig.meta" "cellconfig.setup")

 ; Your egg's license:

 (license "GPL-3")

 ; Pick one from the list of categories (see below) for your egg and
 ; enter it here.

 (category parsing)

 ; A list of eggs npcloud depends on.

 (needs matchable datatype)

 (author "Ivan Raikov")

 (synopsis "Basic routines for reading cell configuration data for neural network models."))

