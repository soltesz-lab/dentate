; -*- Hen -*-

((egg "npcloud.egg") ; This should never change

 ; List here all the files that should be bundled as part of your egg.  

 (files "npcloud.meta" "npcloud.setup" )

 ; Your egg's license:

 (license "GPL-3")

 ; Pick one from the list of categories (see below) for your egg and
 ; enter it here.

 (category parsing)

 ; A list of eggs npcloud depends on.

 (needs make matchable regex
	datatype digraph graph-bfs graph-dfs graph-cycles 
	mathh getopt-long typeclass kd-tree mpi)

 (author "Ivan Raikov")

 (synopsis "A library for neuroanatomical structures and geometric connectivity."))

